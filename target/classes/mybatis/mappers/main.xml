<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
 <mapper namespace="mapper.main">
	
<!--   	<resultMap id="mainResult" type="imageResult">
		<result property="movie_name" column="movie_name"></result>
		<result property="participants" column="PARTICIPANTS"></result>
		<result property="total_score" column="TOTAL_SCORE"></result>
		<result property="image_name" column="image_name"></result>
	</resultMap> -->
	
	
	
<!--  	<select id="selectMovieName" resultType="String">
		<![CDATA[
			select movie_name from total ORDER BY total_score DESC
		]]>
	</select>
	
  	 	<select id="selectImageName" resultType="String">
		<![CDATA[
			select image_name from total ORDER BY total_score DESC
		]]>
	</select>  -->
	
	</mapper>